---
aliases: []
source:
  - https://disk.yandex.com/d/VY1M3LvZAW-7gQ
Вариант: 3, 1
created: 2025-04-01
modified: 2025-07-13
tags: [чисмет]
---
# Задание 1

Вариант 3 ($A=3$)

## Постановка

Для функции $f(x)= \sqrt{1+3cos(x)}$ вычислить приближенное значение интеграла на отрезке $[a,b] = [0, 1]$ используя известные составные квадратурные формулы:

1) Средних прямоугольников
2) Трапеций
3) Симпсона
4) Ньютона-Котеса с шагом $h=0.1$
5) Ньютона-Котеса с шагом $h=0.05$
Оценить погрешность каждой из формул

## 1 Формула средних прямоугольников

### Вычисление

Формула строится по одному узлу $x_0 = \frac{a+b}{2} = \frac {0 + 1}{2} = 0.5 \in [0, 1]$, имеет вид:

$$
S_0[f]=(b-a)f(x_0)
$$

Полагаем, что $f(x_0) = 1.9059768$

Подставляя значения, получаем приближенное значение искомого интеграла:

$$
I[f]\approx S_0[f] = 1\cdot 1.9059768 = 1.9059768
$$

![[desmos-graph (2).png]]

### Погрешность

По построению формулы, погрешность имеет вид:

$$
R_0[f] = 
\int_a^bR_0(x)dx = 
\int_a^bf(x,x_0)\omega_0(x)dx = 
\int_a^bf'(\xi(x))\omega_0(x)dx 
$$

где $\xi(x) \in [a,b]$

Оценим модуль $R_0[f]$. Производную в точке заменим максимумом производной на $[a,b]$ (полагаем, что $f \in C^1([a,b])$) и вынесем за знак интеграла. Проинтегрировав $\omega_0$ от $a$ до $b$ и упростив, получим оценку:

$$
|R_0[f]| \leq \frac{M_1}{4}(b-a)^2
$$

где $M_1 = \max_{x \in [a,b]} |f'(x)|$ 

---

Производная рассматриваемой функции

$$
f'(x) = -\frac{3sin(x)}{2\sqrt{1+3cos(x)}}
$$

на $[0,1]$ монотонно убывает и принимает неположительные значения, значит достигает своего максимального абсолютного значения в $1$.

Полагаем $\max_{x \in [a,b]} |f'(x)| = |f'(1)| = 0.779659$ = $M_1$

Тогда оценка погрешности составит:

$$
|R_0[f]| \leq \frac{0.779659}{4}(1-0)^2 = 0.194914
$$

### summary

| Формула                 | Приближенное начение $\int_a^bf(x)dx$ | Погрешность формулы |
| ----------------------- | ------------------------------------- | ------------------- |
| средних прямоугольников | 1.9059768                             | 0.194914            |

## 2 формула трапеций

### Вычисление

Формула строится по двум узлам $x_0 = a = 0, x_1 = b = 1$, $x_0, x_1 \in [0, 1]$, имеет вид:

$$
S_1[f] = \frac{b-a}{2}f(a) + \frac{b-a}{2}f(b) = \frac{b-a}{2}(f(a)+f(b))
$$

$f(a) = 2$. Полагаем, $f(b) = 1.61892$

Подставляя значения, получаем приближенное значение искомого интеграла:

$$
I[f]\approx S_0[f] = \frac{1-0}{2}(2+1.61892) = 1.80946
$$

![[desmos-graph (3).png]]

### Погрешность

По построению формулы, погрешность имеет вид:

$$
R_1[f] = 
\int_a^bR_1(x)dx = 
\int_a^bf(x,a,b)\omega_1(x)dx = 
\int_a^bf(x,a,b)(x-a)(x-b)dx
$$

Положим $f \in C^2([a,b])$. Оценим модуль $R_1[f]. Заменим разделенную разность отношением второй ее производной к факториалу. Оценим сверху вторую производную в точке из $[a,b]$ максимумом второй производной на $[a,b]$. Вынесем константы. Проинтегрируем оставщуюся под интегралом $w_1 = (x-a)(x-b)$. Упростим. Получим оценку:

$$
|R_1[f]| \leq \frac{M_2}{12}(b-a)^3
$$

где $M_2 = max_{x\in[a,b]}|f''(x)|$

---

Вторая производная рассматриваемой функции

$$
f''(x) = -\frac{6cos(x) + 9 (1+cos^2(x))}{\sqrt{1+3cos(x)}(4+12cos(x))}
$$

не возрастает на $[0,1]$, принимает отрицательные значения. Значит достигает своего абсолютного максимума в точке $1$.

Полагаем $\max_{x \in [a,b]} |f''(x)| = |f''(1)| = 0.87609 = M_2$

Тогда оценка погрешности составит:

$$
|R_1[f]| \leq \frac{M_2}{12}(b-a)^3= \frac{0.87609}{12} \approx 0.073
$$

### summary

| Формула                 | Приближенное начение $\int_a^bf(x)dx$ | Погрешность формулы |
| ----------------------- | ------------------------------------- | ------------------- |
| средних прямоугольников | 1.9059768                             | 0.194914            |
| трапеций                | 1.80946                               | 0.073               |

## 3 формула Симпсона

### Вычисление

Формула строится по трем узлам $x_0 = a = 0, x_1 = \frac{a+b}{2} = 0.5, x_2 = b = 1$, $x_0, x_1, x_2 \in [0, 1]$, имеет вид:

$$
S_2[f] = \frac{b-a}{6}(f(a)+4f(\frac{a+b}{2})+f(b))
$$

Значение функции в узлах были выписаны выше. Подставим в формулу. Получим приближенное значение интеграла:

$$
I[f] = S_2[f] = \frac{1}{6}(2 + 4\cdot 1.9059768 + 1.61892) \approx 1.8738
$$

### Погрешность

## 4 формула Ньютона-Котеса с шагом $h=0.1$

## 5 формула Ньютона-Котеса с шагом $h=0.05$

## Задание 2

## Постановка

[desmos](https://www.desmos.com/calculator/awqrrksw4u)
